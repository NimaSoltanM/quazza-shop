export const publicRoutes = ['/', '/about', '/products', '/products/*'];

export const apiAuthPrefix = '/api/auth';
export const DEFAULT_LOGIN_REDIRECT = '/profile';

export const adminRoutes = ['/admin'];
